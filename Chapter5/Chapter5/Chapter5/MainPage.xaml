<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Chapter5"
             x:Class="Chapter5.MainPage">

    <StackLayout>
        <!-- Place new controls here -->
        <ListView x:Name="simpleListView"
                  Margin="20"
                  ItemTapped="SimpleListView_ItemTapped"
                  ItemSelected="SimpleListView_ItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding FirstName}"
                              Detail="{Binding LastName}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Text="Clear Selection" 
                Clicked="Button_Clicked" />
    </StackLayout>

</ContentPage>
